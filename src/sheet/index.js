import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  icon: {
    color: '#464547',
  },
  centredView: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
  },
});

module.exports = {
  globalStyles: styles,
};
